import React from "react"

export const Projects = ({id}:any) => {
    return(
        <div id={id}>
            <p>{React.createElement('h1', null, '<Projetos />')}</p>
        </div>
        
    )
}